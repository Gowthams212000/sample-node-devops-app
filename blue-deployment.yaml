apiVersion: apps/v1
kind: Deployment
metadata:
  name: sample-app-blue
spec:
  replicas: 2
  selector:
    matchLabels:
      app: sample-app
      version: blue
  template:
    metadata:
      labels:
        app: sample-app
        version: blue
    spec:
      containers:
      - name: app
        image: sample-app:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 8080
